import { MapWidget } from "./components/MapWidget";
import { FC } from "react";

const App: FC = () => {
  return (
    // <MapWidget />
    // for stop the build
    <></>
  );
};

export default App;
